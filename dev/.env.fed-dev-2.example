# Federation Test Environment - Instance 2 (Optional Configuration)
#
# このファイルは環境変数のカスタマイズ用テンプレートです。
# デフォルト値がdocker-compose.ymlに設定されているため、
# もしOrbStack環境であればこのファイルなしで即座に起動できます。
#
# カスタマイズが必要な場合のみ、このファイルをコピーして使用してください：
# cp dev/.env.fed-dev-2.example dev/.env.fed-dev-2

# === ドメイン設定（カスタマイズする場合のみ） ===
# デフォルト: https://nginx-feddev.illo-feddev-2.orb.local
# BASE_URL=https://your-custom-domain.com
# FRONTEND_URL=https://your-custom-domain.com
# MINIO_PUBLIC_URL=https://your-custom-domain.com

# === インスタンス識別子 ===
# Cookie名前空間などに使用
# INSTANCE_ID=feddev2
# NUXT_PUBLIC_INSTANCE_ID=feddev2

# === セキュリティキー（本番環境では必須） ===
# 開発環境ではデフォルト値が使用されますが、本番環境では必ず生成してください

# !!注意!! feddev1とは異なるキーを設定してください

# JWT Secret (Generate: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))")
# JWT_SECRET=

# Encryption Key (Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# ENCRYPTION_KEY=

# Image Encryption Key (Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# IMAGE_ENCRYPTION_KEY=

# Message Encryption Key (Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# MESSAGE_ENCRYPTION_KEY=

# Image Signing Key (Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# IMAGE_SIGNING_KEY=

# === 画像セキュリティ設定 ===
# IMAGE_SIGNED_URL_ENABLED=true
# IMAGE_SIGNED_URL_EXPIRES=30
# IMAGE_HOTLINK_PROTECTION=true
# IMAGE_ALLOW_NO_REFERER=false

# === 画像処理設定 ===
# IMAGE_MAX_FILE_SIZE_MB=32
# IMAGE_MAX_TOTAL_SIZE_MB=200
# IMAGE_MAX_FILES_PER_ARTWORK=20
# IMAGE_DEFAULT_MAX_WIDTH=2048
# IMAGE_PRESERVE_FORMAT=true
# IMAGE_EMBED_METADATA=true
# IMAGE_SUPPORT_SVG=true

# === ストレージクォータ ===
# STORAGE_QUOTA_DEFAULT_GB=1
# STORAGE_QUOTA_ADMIN_GB=10
# STORAGE_QUOTA_ENFORCE=false

# === Bluesky OAuth 設定 ===
# 公開URLでない限りは無効
# node scripts/js/generate-bluesky-jwks.js
# BLUESKY_OAUTH_ENABLED=true
# BLUESKY_OAUTH_PRIVATE_JWK=

# === インスタンス連絡先情報 ===
# ユーザーに表示される連絡先情報（メール、URL、テキスト）
# INSTANCE_CONTACT_INFO=admin@localhost
# 管理者ユーザー名（@なし）- ユーザープロフィールへのリンクに使用
# INSTANCE_ADMIN_USERNAME=
