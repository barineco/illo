# Local Development Environment (Optional Configuration)
#
# このファイルは環境変数のカスタマイズ用テンプレートです。
# dev/.env.local-dev が存在する場合、ホストマシン上で実行される
# backend/frontend プロセスがこのファイルを読み込みます。
#
# デフォルト値があるため、基本的にはこのファイルなしで起動できます。
# カスタマイズが必要な場合のみ、このファイルをコピーして使用してください：
# cp dev/.env.local-dev.example dev/.env.local-dev

# === データベース設定（docker-compose.local-dev.ymlでデフォルト値あり） ===
# デフォルト: postgresql://illustboard_user:illustboard_pass@localhost:5432/illustboard_db
#DATABASE_URL=postgresql://illustboard_user:illustboard_pass@localhost:5432/illustboard_db

# === Redis設定 ===
# デフォルト: localhost:6379
# REDIS_HOST=localhost
# REDIS_PORT=6379

# === MinIO/S3設定 ===
# デフォルト: localhost:9000, minioadmin/minioadmin
# MINIO_ENDPOINT=localhost
# MINIO_PORT=9000
# MINIO_USE_SSL=false
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=illustboard
# MINIO_PUBLIC_URL=http://localhost:9000

# === アプリケーション設定 ===
# デフォルト: http://localhost:11103
# BASE_URL=http://localhost:11103
# FRONTEND_URL=http://localhost:11103
# PORT=11104
# NODE_ENV=development

# === インスタンスブランディング ===
# UI左上、メール、2FAなどに表示されるサービス名
# INSTANCE_NAME=illo
# ブラウザタイトルに表示されるタグライン（オプション）
# INSTANCE_TAGLINE=

# === インスタンス識別子 ===
# Cookie名前空間などに使用。複数インスタンス利用時に変更
# INSTANCE_ID=local-dev
# NUXT_PUBLIC_INSTANCE_ID=local-dev

# === セキュリティキー（開発環境用） ===
# 開発環境ではプロジェクトルートの.envの値が使用されます
# カスタマイズする場合のみ設定してください

# JWT Secret
# JWT_SECRET=dev_secret_change_in_production

# Encryption Key (64-character hex string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ENCRYPTION_KEY=

# Image Encryption Key
# IMAGE_ENCRYPTION_KEY=

# Message Encryption Key
#MESSAGE_ENCRYPTION_KEY=

# Image Signing Key
# IMAGE_SIGNING_KEY=

# === 画像セキュリティ設定 ===
# ホットリンク保護と署名付きURL
# IMAGE_HOTLINK_PROTECTION=true
# IMAGE_ALLOW_NO_REFERER=false
# IMAGE_SIGNED_URL_ENABLED=true
# IMAGE_SIGNED_URL_EXPIRES=300

# === 画像処理設定 ===
# IMAGE_MAX_FILE_SIZE_MB=32
# IMAGE_MAX_TOTAL_SIZE_MB=200
# IMAGE_MAX_FILES_PER_ARTWORK=20
# IMAGE_DEFAULT_MAX_WIDTH=2048
# IMAGE_PRESERVE_FORMAT=true
# IMAGE_EMBED_METADATA=true
# IMAGE_SUPPORT_SVG=true

# === ストレージクォータ ===
# STORAGE_QUOTA_DEFAULT_GB=1
# STORAGE_QUOTA_ADMIN_GB=10
# STORAGE_QUOTA_ENFORCE=false

# === メール設定（MailHogを使用） ===
# デフォルト: localhost:1025
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_SECURE=false
# MAIL_FROM=noreply@localhost

# === Bluesky OAuth 設定 ===
# 開発環境でBluesky OAuthを使用する場合はこれらを設定
# BLUESKY_OAUTH_PRIVATE_JWKは以下のコマンドで生成できます:
# node scripts/js/generate-bluesky-jwks.js
# BLUESKY_OAUTH_ENABLED=true
# BLUESKY_OAUTH_PRIVATE_JWK=

# === レート制限（アンチスクレイピング）設定 ===
# 計測モード: ペナルティなしでベースライン情報を収集
# RATE_LIMIT_MEASUREMENT_MODE=false

# 複合スコアアルゴリズム（CV + 間隔分析）を有効化
# RATE_LIMIT_USE_COMPOSITE_SCORE=false

# スコア閾値（0-100、RATE_LIMIT_USE_COMPOSITE_SCORE=true時のみ有効）
# HARD_LIMIT: 完全ブロック（デフォルト: 90）
# SOFT_LIMIT: 画質低下（デフォルト: 50）
# WARNING: ログのみ（デフォルト: 35）
# RATE_LIMIT_HARD_SCORE=90
# RATE_LIMIT_SOFT_SCORE=50
# RATE_LIMIT_WARNING_SCORE=35
